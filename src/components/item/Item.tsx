import React from 'react'
import { Wrapper } from '../../App.styles';
import { CartItemType } from '../../types/CartItem';

type ItemProps = {
    item: CartItemType;
}

const Item = () => {
  return (
    <div>
        Item
    </div>
  )
}

export default Item;